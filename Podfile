# Uncomment this line to define a global platform for your project
# platform :ios, ‘8.0’

target 'PGMultifunctionTextFieldView' do

pod 'Masonry'
pod 'IQKeyboardManager'

  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for PGMultifunctionTextFieldView

  target 'PGMultifunctionTextFieldViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PGMultifunctionTextFieldViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
